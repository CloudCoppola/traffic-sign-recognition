# Traffic Sign Recognition

## Overview
This project implements a Convolution Neural Network (CNN) for traffic sign classification using Tensorflow/Keras.

## Goals
- Build a reproducible ML pipeline
- Train and evaluate a CNN on traffic sign data 
- Explore preprocessing and augmentation strategies

## Project

## Setup
```bash
git clone ...
cd traffic-sign-recognition
pip install -r requirements.txt
```
